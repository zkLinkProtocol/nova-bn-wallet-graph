type AquaCToken @entity(immutable: false) {
  # cToken id
  id: Bytes!

  balance: BigInt!

  totalSupplied: BigInt!

  blockNumber: BigInt!
}

type UserPosition @entity(immutable: false) {
  # account Address
  id: Bytes!

  positions: [TokenPosition!]! @derivedFrom(field: "userPosition")
}

type TokenPosition @entity(immutable: false) {
  # user account + token address
  id: Bytes!
  # underlying token address
  token: Bytes!
  # pool address
  pool: Bytes!
  # underlying balance
  supplied: BigInt!
  # underlying token decimal
  decimal: BigInt!
  # user account
  userPosition: UserPosition!
  # hash
  transactionHash: Bytes!
  # block number
  blockNumber: BigInt!
}
